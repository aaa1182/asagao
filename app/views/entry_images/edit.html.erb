<% @page_title = 'ブログ記事の画像編集う' %>
<h1><%= @page_title %></h1>
<h2><%= @entry.title %></h2>

<% url = entry_image_path(@entry, @image) %>
<%= form_for @image, as:"image", url: url do %>
  <%= render "form", form: fomr %>
  <div><%= form.submit %></div>
<% end %>
